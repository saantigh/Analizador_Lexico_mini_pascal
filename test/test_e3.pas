program SemanticErr;
var
  x: integer;
  s: string;
begin
  x := 'Hello';          { Assigning string to integer }
  writeln(y);            { 'y' is not declared }
  s := s + 1;            { Adding integer to string }
end.

